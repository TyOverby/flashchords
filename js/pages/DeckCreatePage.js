import { html } from '../deps.js';

export default function DeckCreatePage() {
  return html`
    <div class="stack">
      <a href="#/" class="back-link">← Back</a>
      <p class="text-center text-muted">Deck creation — coming soon</p>
    </div>
  `;
}
