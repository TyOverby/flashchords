import { html } from '../deps.js';

export default function ChordListPage() {
  return html`
    <div class="stack">
      <a href="#/" class="back-link">← Back</a>
      <p class="text-center text-muted">Chord list — coming soon</p>
    </div>
  `;
}
